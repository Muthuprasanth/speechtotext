import { IAuthOptions } from 'node-sp-auth';
export declare class Download {
    private spr;
    private context;
    private agent;
    constructor(context: IAuthOptions);
    downloadFile: (spFileAbsolutePath: string, saveTo?: string) => Promise<any>;
    downloadFileFromSite: (siteUrl: string, spRelativeFilePath: string, saveTo?: string) => Promise<any>;
    private downloadFileAsStream;
    private getWebByAnyChildUrl;
    private initContext;
    private promptForCreds;
    private getSaveFilePath;
}
