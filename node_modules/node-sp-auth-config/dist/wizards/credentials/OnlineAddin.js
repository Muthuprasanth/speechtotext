"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const inquirer = require("inquirer");
const utils_1 = require("../../utils");
const wizard = (authContext, answersAll = {}, settings = {}) => {
    let onlineAddinCredentials = authContext.authOptions;
    let promptFor = [
        {
            name: 'clientId',
            message: 'clientId',
            type: 'input',
            default: onlineAddinCredentials.clientId,
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }, {
            name: 'clientSecret',
            message: 'clientSecret',
            type: 'password',
            default: onlineAddinCredentials.clientSecret ? utils_1.defaultPasswordMask : null,
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }
    ];
    return inquirer.prompt(promptFor)
        .then((answers) => {
        return Object.assign({}, answersAll, answers, { clientSecret: answers.clientSecret === utils_1.defaultPasswordMask
                ? onlineAddinCredentials.clientSecret
                : answers.clientSecret });
    });
};
exports.default = wizard;

//# sourceMappingURL=OnlineAddin.js.map
