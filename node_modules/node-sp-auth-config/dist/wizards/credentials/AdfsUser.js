"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const inquirer = require("inquirer");
const utils_1 = require("../../utils");
const wizard = (authContext, answersAll = {}, settings = {}) => {
    let adfsUserCredentials = authContext.authOptions;
    let promptFor = [
        {
            name: 'username',
            message: 'User name',
            type: 'input',
            default: adfsUserCredentials.username,
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }, {
            name: 'password',
            message: 'Password',
            type: 'password',
            default: adfsUserCredentials.password ? utils_1.defaultPasswordMask : null,
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }, {
            name: 'relyingParty',
            message: 'relyingParty',
            type: 'input',
            default: adfsUserCredentials.relyingParty || 'urn:sharepoint:portal',
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }, {
            name: 'adfsUrl',
            message: 'adfsUrl',
            type: 'input',
            default: adfsUserCredentials.adfsUrl,
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }, {
            name: 'adfsCookie',
            message: 'adfsCookie',
            type: 'input',
            default: adfsUserCredentials.adfsCookie || 'FedAuth',
            validate: (answer) => {
                if (answer.length === 0) {
                    return false;
                }
                return true;
            }
        }
    ];
    return inquirer.prompt(promptFor)
        .then((answers) => {
        return Object.assign({}, answersAll, answers, { password: answers.password === utils_1.defaultPasswordMask
                ? adfsUserCredentials.password
                : answers.password });
    });
};
exports.default = wizard;

//# sourceMappingURL=AdfsUser.js.map
